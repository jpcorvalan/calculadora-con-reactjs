import { createContext } from 'react';

// Inicializamos nuestro contexto con un objeto vacío, que luego contendrá variables de estado y funciones
const ListaGuardadosContext = createContext({});

export { ListaGuardadosContext }